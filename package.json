{
  "name": "chatnest-extension",
  "version": "1.2.0",
  "description": "为 AI 对话平台提供极简侧边导航",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp manifest.json dist/ && cp -r icons dist/ && cp src/popup/popup.html dist/popup.html && cp src/popup/popup.css dist/popup.css && cp src/popup/popup.js dist/popup.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.0.0"
  }
}
